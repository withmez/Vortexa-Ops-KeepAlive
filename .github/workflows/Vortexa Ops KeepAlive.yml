name: Vortexa Ops KeepAlive

on:
  schedule:
    # 北京时间每 3 天的清晨 06:00 自动运行一次
    - cron: '0 22 */3 * *'
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install playwright requests
        python -m playwright install chromium --with-deps

    - name: Run Ops KeepAlive
      env:
        VORTEXA_EMAIL: ${{ secrets.VORTEXA_EMAIL }}
        VORTEXA_PASSWORD: ${{ secrets.VORTEXA_PASSWORD }}
        TG_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TG_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        python - calculus - << 'EOF'
        import os
        import sys
        import time
        import html
        import re
        import requests
        from datetime import datetime
        from playwright.sync_api import sync_playwright

        EMAIL = os.getenv("VORTEXA_EMAIL")
        PASSWORD = os.getenv("VORTEXA_PASSWORD")
        TG_TOKEN = os.getenv("TG_BOT_TOKEN")
        TG_CHAT_ID = os.getenv("TG_CHAT_ID")
        
        # 🎯 策略微调：第一步先进入大厅服务列表页
        BASE_URL = "https://www.vortexa.cloud/services"

        def tg(msg):
            if not TG_TOKEN or not TG_CHAT_ID:
                print("TG 未配置")
                return
            url = f"https://api.telegram.org/bot{TG_TOKEN}/sendMessage"
            try:
                requests.post(url, json={
                    "chat_id": TG_CHAT_ID,
                    "text": msg,
                    "parse_mode": "HTML",
                    "disable_web_page_preview": True
                }, timeout=20)
            except Exception as e:
                print(f"TG 发送失败: {e}")

        def now():
            return datetime.utcnow().strftime("%Y-%m-%d %H:%M:%S UTC")

        print("BOOT OPS SYSTEM - AUTOMATIC NODE DETECTION")

        with sync_playwright() as p:
            browser = p.chromium.launch(
                headless=True,
                args=["--no-sandbox", "--disable-dev-shm-usage", "--disable-gpu"]
            )
            context = browser.new_context(
                viewport={"width": 1440, "height": 900},
                user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36"
            )
            page = context.new_page()

            def core_keepalive_flow():
                print("🌐 正在连接主服务列表页面...")
                page.goto(BASE_URL, wait_until="domcontentloaded", timeout=45000)
                page.wait_for_timeout(4000)

                # 1. 自动登录拦截
                if page.locator("input[type='password']").count() > 0 or "login" in page.url.lower():
                    print("🔑 LOGIN REQUIRED - 触发自动登录...")
                    page.locator("input[type='email'], input[name='email']").first.fill(EMAIL)
                    page.locator("input[type='password'], input[name='password']").first.fill(PASSWORD)
                    
                    submit_btn = page.locator("button[type='submit'], button:has-text('Log In'), button:has-text('Sign In')").first
                    submit_btn.click()
                    
                    page.wait_for_load_state("networkidle", timeout=45000)
                    page.wait_for_timeout(5000)

                # 确保登录完成后留存在列表页
                if "login" in page.url.lower():
                    raise Exception("登录跳转失败，请检查账号密码配置。")

                if "/services" not in page.url:
                    page.goto(BASE_URL, wait_until="networkidle", timeout=45000)
                    page.wait_for_timeout(4000)

                # 2. 🔍 核心动态识别：扫描当前所有状态，自动挑出真正的活跃服务
                print("⚡ 开始盲搜当前账户下的活跃节点...")
                active_badges = page.locator("text=/^(Active|Running)$/i")
                active_count = active_badges.count()
                
                detected_node = None
                
                for i in range(active_count):
                    # 向上穿透拿到该状态所在的整行或整块卡片
                    container = active_badges.nth(i).locator("xpath=ancestor::tr | ancestor::div[contains(@class,'card')] | ancestor::div[./a[contains(@href, '/services/')]]").first
                    container_text = container.inner_text()
                    
                    # 排除掉被注销的死节点
                    if "cancelled" in container_text.lower() or "suspended" in container_text.lower():
                        continue
                    
                    # 提取该区域内有效的详情页链接
                    target_link = container.locator("a[href*='/services/']").first
                    if target_link.count() > 0:
                        href = target_link.get_attribute("href") or ""
                        m = re.search(r"/services/(\d+)", href)
                        if m:
                            detected_node = m.group(1)
                            print(f"🎯 成功自动识别到当前活跃节点: #{detected_node}")
                            break

                # 保底兜底：如果卡片组件太复杂没抓到，找第一个不含 cancelled 关键字的链接
                if not detected_node:
                    print("⚠️ 未能通过状态块精确匹配，启动正常存活实例搜索保底...")
                    all_links = page.locator("a[href*='/services/']")
                    for i in range(all_links.count()):
                        link_locator = all_links.nth(i)
                        href = link_locator.get_attribute("href") or ""
                        m = re.search(r"/services/(\d+)", href)
                        if m:
                            row_text = link_locator.evaluate("el => el.closest('tr')?.innerText || el.closest('div')?.innerText || el.innerText").lower()
                            if not any(x in row_text for x in ["cancelled", "suspended", "expired"]):
                                detected_node = m.group(1)
                                print(f"保底命中正常节点: #{detected_node}")
                                break

                if not detected_node:
                    raise Exception("在当前账户中未发现任何处于 Active 状态的有效服务。")

                # 3. 跨入动态识别到的最新活鸡详情页，触发终极保活巡检
                target_url = f"https://www.vortexa.cloud/services/{detected_node}"
                print(f"🚀 正在切入目标活跃服务详情页: {target_url}")
                page.goto(target_url, wait_until="networkidle", timeout=45000)
                page.wait_for_timeout(4000)

                print("⚙️ 已成功进入服务详情页，账号保活访问已触发。")

                # 4. 抓取创建时间并推算剩余天数
                print("⏳ 正在提取机器到期指标...")
                info_block = page.locator("div:has-text('Created:')").last
                info_text = info_block.inner_text() if info_block.count() > 0 else page.locator("body").inner_text()
                
                clean_log_text = info_text.replace('\n', ' ')
                print(f"基础文本快照: {clean_log_text}")

                date_match = re.search(r'Created:\s*([A-Za-z]+\s+\d{1,2},\s*\d{4})', info_text)
                if not date_match:
                    date_match = re.search(r'([A-Za-z]+\s+\d{1,2},\s*\d{4})', info_text)

                if date_match:
                    created_date_str = date_match.group(1).replace(',', '')
                    created_date = datetime.strptime(created_date_str, "%b %d %Y")
                    expiry_date = created_date.replace(year=created_date.year + 1)
                    
                    delta = expiry_date - datetime.utcnow()
                    remaining_days = max(0, delta.days)
                    print(f"📆 机器创建于: {created_date_str} -> 推算到期日: {expiry_date.strftime('%Y-%m-%d')} -> 剩余: {remaining_days} 天")
                    return remaining_days, detected_node, target_url
                else:
                    page.screenshot(path="error.png", full_page=True)
                    raise Exception("未能成功解析出详情页的创建日期，无法计算剩余天数。")

            try:
                days = None
                active_id = None
                active_url = None
                max_retries = 3
                
                for attempt in range(max_retries):
                    try:
                        days, active_id, active_url = core_keepalive_flow()
                        break
                    except Exception as flow_err:
                        print(f"⚠️ 第 {attempt + 1} 次尝试失败: {flow_err}")
                        if attempt == max_retries - 1:
                            raise
                        time.sleep(10)

                ts = now()
                status_color = "🟢 正常" if days > 30 else "🟠 警告" if days > 10 else "🔴 临界"

                msg = "<b>🖥 VORTEXA 自动保活监控</b>\n\n"
                msg += f"<b>运行状态</b>\n{status_color}\n\n"
                msg += f"<b>剩余续期</b>\n<b>{days} 天</b>\n\n"
                msg += f"<b>服务节点</b>\n<code>#{active_id}</code>\n\n"
                msg += f"<b>控制面板</b>\n<a href=\"{active_url}\">打开当前服务内页</a>\n\n"
                msg += f"<b>检测时间</b>\n<code>{ts}</code>"

                tg(msg)
                print("SUCCESS: MONITOR CARD SENT")

            except Exception as e:
                err_str = str(e)
                print(f"CRASH: {err_str}")
                try:
                    page.screenshot(path="error.png", full_page=True)
                except:
                    pass

                safe_error = html.escape(err_str[:250])
                fail_msg = "<b>🚨 VORTEXA 保活运行失败</b>\n\n"
                fail_msg += f"<b>失败时间</b>\n<code>{now()}</code>\n\n"
                fail_msg += f"<b>错误日志</b>\n<code>{safe_error}</code>\n\n"
                fail_msg += "<b>处理建议</b>\n请前往 Actions 查看 error.png 截图确定原因。"

                tg(fail_msg)
                sys.exit(1)
            finally:
                browser.close()
        EOF

    - name: Upload Error Screenshot
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: error-screenshot
        path: error.png
        if-no-files-found: ignore
